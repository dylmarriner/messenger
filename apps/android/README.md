# Android client

Native Android boundary: Kotlin, Jetpack Compose, Android Keystore and encrypted local persistence.

The Android application will be generated after the Rust crypto/provider API is stable enough to expose through FFI. Do not duplicate cryptographic protocol logic in Kotlin.
