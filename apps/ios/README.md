# iOS client

Native iOS boundary: Swift, SwiftUI, Keychain/Secure Enclave where applicable and encrypted local persistence.

The iOS application will be generated after the Rust crypto/provider API is stable enough to expose through FFI. Do not duplicate cryptographic protocol logic in Swift.
